async function searchInventory(){const q=document.getElementById('invQ').value.trim();const res=await fetch('/api/inventory/search?q='+encodeURIComponent(q));const data=await res.json();if(!data.ok){showToast(data.error,false);return;}let html='<div class="card p-2"><table class="table table-sm table-pda"><thead><tr><th>Loc</th><th>SKU</th><th>Barcode</th><th>Qty</th></tr></thead><tbody>';for(const r of data.data.rows){html+=`<tr><td>${r.location_id}</td><td>${r.sku}</td><td>${r.barcode}</td><td>${r.qty_onhand}</td></tr>`;}html+='</tbody></table></div>';document.getElementById('invResult').innerHTML=html;}
