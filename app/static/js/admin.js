async function loadPutaway(){const pallet=document.getElementById('paScan').value.trim();if(!pallet){showToast('Scan PA trước',false);return;}const res=await fetch('/api/putaway/task/'+encodeURIComponent(pallet));const data=await res.json();if(!data.ok){showToast(data.error,false);return;}const t=data.data;document.getElementById('queue_id').value=t.queue_id;document.getElementById('taskBox').classList.remove('d-none');document.getElementById('putawayForm').classList.remove('d-none');document.getElementById('taskBox').innerHTML=`<b>PO:</b> ${t.po_no}<br><b>PA:</b> ${t.pallet_id}<br><b>SKU:</b> ${t.sku}<br><b>Barcode:</b> ${t.barcode}<br><b>GR:</b> ${t.qty_gr} | <b>Put:</b> ${t.qty_putaway} | <b>Remain:</b> ${t.qty_remain_putaway}<br><span class="badge bg-warning text-dark badge-big">${t.flow_status}</span>`;document.querySelector("input[name='location_id']").focus();}
bindAjaxForm('putawayForm','/api/putaway/confirm');
